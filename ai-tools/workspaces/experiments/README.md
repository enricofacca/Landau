# Experimental workspaces

Risky or competing implementations live here under stable study/attempt IDs. Each independent attempt gets its own directory.

Promotion to `src/` requires reviewed evidence and permanent public tests in `tests/`.
